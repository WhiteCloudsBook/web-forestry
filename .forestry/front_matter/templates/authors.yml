---
label: Authors
hide_body: true
fields:
- name: authors
  type: field_group_list
  fields:
  - name: name
    type: text
    config:
      required: true
    label: author name
  config:
    min: 
    max: 
    labelField: 
  label: authors
pages:
- content/data/authors.yaml
